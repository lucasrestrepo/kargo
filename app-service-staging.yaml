apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sample-service-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/your-repo.git'  # Replace with your repository URL
    targetRevision: HEAD
    path: k8s/overlays/staging
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: staging-namespace  # Replace with the target namespace for staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
